@using SGRRHH.Local.Domain.Enums
@using SGRRHH.Local.Shared.Interfaces
@inject IAuthService AuthService

@* =====================================================================
   ROLES TEMPORALMENTE DESHABILITADOS - Todos los usuarios autenticados 
   tienen acceso completo mientras se rediseña el sistema de roles
   ===================================================================== *@

@if (IsAuthorized)
{
    @Authorized
}
else
{
    @NotAuthorized
}

@code {
    [Parameter] public RenderFragment? Authorized { get; set; }
    [Parameter] public RenderFragment? NotAuthorized { get; set; }
    
    /// <summary>
    /// [OBSOLETO] El parámetro RequiredRole se ignora temporalmente.
    /// Todos los usuarios autenticados tienen acceso completo.
    /// </summary>
    [Parameter] public RolUsuario? RequiredRole { get; set; }
    
    /// <summary>
    /// Mientras el sistema de roles está deshabilitado, solo se verifica autenticación.
    /// </summary>
    private bool IsAuthorized => AuthService.IsAuthenticated;
}
