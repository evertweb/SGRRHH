<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Brushes.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <!-- ============================================== -->
    <!-- ESTILOS DE FORMULARIOS - SGRRHH                -->
    <!-- TextBox, ComboBox, DatePicker                  -->
    <!-- ============================================== -->
    
    <!-- === TEXTBOX ESTÁNDAR === -->
    <Style x:Key="StandardTextBox" TargetType="TextBox">
        <Setter Property="Height" Value="35"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
    </Style>
    
    <!-- TextBox con placeholder (usando VisualBrush technique) -->
    <Style x:Key="SearchTextBox" TargetType="TextBox" BasedOn="{StaticResource StandardTextBox}">
        <Setter Property="Width" Value="250"/>
        <Style.Triggers>
            <Trigger Property="Text" Value="">
                <Setter Property="Background">
                    <Setter.Value>
                        <VisualBrush Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock Text="🔍 Buscar..." 
                                           Foreground="{StaticResource TextDisabledBrush}" 
                                           Margin="10,0,0,0" FontSize="13"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- === COMBOBOX ESTÁNDAR === -->
    <Style x:Key="StandardComboBox" TargetType="ComboBox">
        <Setter Property="Height" Value="35"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="10,0"/>
    </Style>
    
    <!-- === DATEPICKER ESTÁNDAR === -->
    <Style x:Key="StandardDatePicker" TargetType="DatePicker">
        <Setter Property="Height" Value="35"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    
    <!-- === TEXTBOX PARA CHAT (Tema Oscuro) === -->
    <Style x:Key="ChatTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource ChatSurfaceDarkBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ChatTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ChatBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="CaretBrush" Value="{StaticResource ChatTextBrush}"/>
    </Style>
    
    <!-- === LABEL DE CAMPO === -->
    <Style x:Key="FieldLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>
    
    <Style x:Key="FieldLabelBold" TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}">
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    
    <!-- === ALIAS PARA COMPATIBILIDAD === -->
    <Style x:Key="FormLabel" TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}"/>
    
    <!-- === GRUPO DE CAMPOS (GroupBox alternativo) === -->
    <Style x:Key="FieldGroup" TargetType="StackPanel">
        <Setter Property="Margin" Value="0,0,10,10"/>
    </Style>
    
    <!-- === CHECKBOX ESTILO TARJETA === -->
    <Style x:Key="CheckBoxCardStyle" TargetType="CheckBox">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Margin" Value="0,0,10,10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="5" 
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox IsChecked="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" 
                                      VerticalAlignment="Center" Margin="0,0,10,0" IsHitTestVisible="False"/>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
