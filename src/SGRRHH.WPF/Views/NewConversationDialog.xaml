<Window x:Class="SGRRHH.WPF.Views.NewConversationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Nueva Conversación" Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Botón moderno -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2A2A"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#363636"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox moderno -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#2D2D2D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#E0E0E0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#27282C" Padding="20,16" BorderBrush="#2D2D2D" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="Nueva Conversación" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF"/>
                <TextBlock Text="Selecciona un usuario para iniciar una conversación" FontSize="12" Foreground="#72767D" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Buscador -->
        <Border Grid.Row="1" Padding="16,12" Background="#1E1E1E">
            <TextBox x:Name="SearchTextBox"
                     Style="{StaticResource ModernTextBox}"
                     TextChanged="SearchTextBox_TextChanged">
                <TextBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
        </Border>

        <!-- Lista de usuarios -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Background="#0F0F0F">
            <ItemsControl x:Name="UsersListBox" Padding="0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="Transparent"
                                Cursor="Hand"
                                Padding="20,14"
                                BorderBrush="#2D2D2D"
                                BorderThickness="0,0,0,1"
                                MouseLeftButtonDown="UserItem_Click"
                                Tag="{Binding}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2B2D31"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Avatar -->
                                <Border Grid.Column="0"
                                        Width="42" Height="42"
                                        CornerRadius="21"
                                        Background="#5865F2"
                                        Margin="0,0,12,0">
                                    <TextBlock HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               FontWeight="Bold"
                                               FontSize="16"
                                               Foreground="White">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0}{1}">
                                                <Binding Path="NombreCompleto" Converter="{StaticResource FirstLetterConverter}"/>
                                                <Binding Path="NombreCompleto" Converter="{StaticResource SecondWordFirstLetterConverter}"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </Border>

                                <!-- Nombre y username -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding NombreCompleto}"
                                               FontWeight="SemiBold"
                                               FontSize="14"
                                               Foreground="#FFFFFF"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Username, StringFormat='@{0}'}"
                                               FontSize="12"
                                               Foreground="#72767D"
                                               TextTrimming="CharacterEllipsis"
                                               Margin="0,2,0,0"/>
                                </StackPanel>

                                <!-- Flecha -->
                                <TextBlock Grid.Column="2"
                                           Text="➤"
                                           FontSize="16"
                                           Foreground="#72767D"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer con botón cancelar -->
        <Border Grid.Row="3" Background="#27282C" Padding="16" BorderBrush="#2D2D2D" BorderThickness="0,1,0,0">
            <Button Content="Cancelar"
                    Style="{StaticResource ModernButton}"
                    Click="CancelButton_Click"
                    HorizontalAlignment="Center"
                    Padding="24,10"/>
        </Border>

        <!-- Loading overlay -->
        <Border Grid.Row="0" Grid.RowSpan="4"
                x:Name="LoadingOverlay"
                Background="#CC000000"
                Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="⚡" FontSize="48" HorizontalAlignment="Center" Foreground="White"/>
                <TextBlock Text="Cargando usuarios..."
                           FontSize="16"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
