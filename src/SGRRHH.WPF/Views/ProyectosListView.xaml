<UserControl x:Class="SGRRHH.WPF.Views.ProyectosListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:SGRRHH.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#C0C0C0">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles/LegacyTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <!-- ============================================ -->
        <!-- PANTALLA DE BIENVENIDA (Home) -->
        <!-- ============================================ -->
        <Grid Visibility="{Binding IsHomeVisible, Converter={StaticResource BoolToVisConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="500">
                <!-- Título -->
                <TextBlock Text="[PROYECTOS]" FontFamily="Courier New" FontSize="24" FontWeight="Bold"
                           Foreground="#000080" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="GESTION DE PROYECTOS" FontFamily="Tahoma" FontSize="14" FontWeight="Bold" 
                           Foreground="#000000" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Seleccione una opcion:" FontFamily="Tahoma" FontSize="10" 
                           Foreground="#404040" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <!-- Opciones -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- Opción: Nuevo Proyecto -->
                    <Border Background="White" BorderThickness="2" BorderBrush="#808080"
                            Padding="20,15" Margin="0,0,15,0" Cursor="Hand" Width="180">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding ShowFormCommand}"/>
                        </Border.InputBindings>
                        <StackPanel HorizontalAlignment="Center">
                            <Border Background="#008000" Width="50" Height="50" 
                                    HorizontalAlignment="Center" Margin="0,0,0,10">
                                <TextBlock Text="[+]" FontFamily="Courier New" FontSize="20" FontWeight="Bold"
                                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Nuevo Proyecto" FontFamily="Tahoma" FontSize="10" FontWeight="Bold"
                                       Foreground="#000000" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="Crear un nuevo" FontFamily="Tahoma" FontSize="8"
                                       Foreground="#808080" HorizontalAlignment="Center"/>
                            <TextBlock Text="proyecto" FontFamily="Tahoma" FontSize="8"
                                       Foreground="#808080" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Opción: Ver Proyectos -->
                    <Border Background="White" BorderThickness="2" BorderBrush="#808080"
                            Padding="20,15" Cursor="Hand" Width="180">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding ShowListCommand}"/>
                        </Border.InputBindings>
                        <StackPanel HorizontalAlignment="Center">
                            <Border Background="#000080" Width="50" Height="50" 
                                    HorizontalAlignment="Center" Margin="0,0,0,10">
                                <TextBlock Text="[=]" FontFamily="Courier New" FontSize="20" FontWeight="Bold"
                                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Ver Proyectos" FontFamily="Tahoma" FontSize="10" FontWeight="Bold"
                                       Foreground="#000000" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="Consultar y gestionar" FontFamily="Tahoma" FontSize="8"
                                       Foreground="#808080" HorizontalAlignment="Center"/>
                            <TextBlock Text="proyectos existentes" FontFamily="Tahoma" FontSize="8"
                                       Foreground="#808080" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
            
            <!-- Footer con fecha actual -->
            <Border Grid.Row="1" Background="#E0E0E0" Padding="10,8" BorderThickness="0,1,0,0" BorderBrush="#808080">
                <TextBlock HorizontalAlignment="Center" FontFamily="Tahoma" FontSize="9" Foreground="#404040">
                    <Run Text="Fecha: "/>
                    <Run Text="{Binding CurrentDateText, Mode=OneWay}"/>
                </TextBlock>
            </Border>
        </Grid>
        
        <!-- ============================================ -->
        <!-- VISTA DE FORMULARIO (Crear/Editar) -->
        <!-- ============================================ -->
        <Grid Visibility="{Binding IsFormViewVisible, Converter={StaticResource BoolToVisConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" Background="#000080" Padding="10,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Content="&lt;- Volver" 
                            Command="{Binding ShowHomeCommand}"
                            Style="{StaticResource LegacyButtonStyle}"
                            FontFamily="Tahoma" FontSize="9" Padding="10,3" Margin="0,0,15,0"/>
                    
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontFamily="Tahoma" FontSize="11" FontWeight="Bold" Foreground="White">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="[+] NUEVO PROYECTO"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                            <Setter Property="Text" Value="[Ed] EDITAR PROYECTO"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Contenido del formulario -->
            <Border Grid.Row="1" BorderThickness="1" BorderBrush="#808080" Background="White" Margin="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header del panel -->
                    <Border Grid.Row="0" Background="#000080" Padding="8,5">
                        <TextBlock Text="DATOS DEL PROYECTO" FontFamily="Tahoma" FontSize="9" 
                                   FontWeight="Bold" Foreground="White"/>
                    </Border>
                    
                    <!-- Campos -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15">
                        <StackPanel MaxWidth="700">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Codigo *" FontFamily="Tahoma" FontSize="9" 
                                               Foreground="#404040" Margin="0,0,0,3"/>
                                    <TextBox Text="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource LegacyTextBoxStyle}"
                                             FontFamily="Tahoma" FontSize="9" Height="22"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Nombre *" FontFamily="Tahoma" FontSize="9" 
                                               Foreground="#404040" Margin="0,0,0,3"/>
                                    <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource LegacyTextBoxStyle}"
                                             FontFamily="Tahoma" FontSize="9" Height="22"/>
                                </StackPanel>
                            </Grid>
                            
                            <StackPanel Margin="0,12,0,0">
                                <TextBlock Text="Cliente" FontFamily="Tahoma" FontSize="9" 
                                           Foreground="#404040" Margin="0,0,0,3"/>
                                <TextBox Text="{Binding Cliente, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource LegacyTextBoxStyle}"
                                         FontFamily="Tahoma" FontSize="9" Height="22"/>
                            </StackPanel>
                            
                            <StackPanel Margin="0,12,0,0">
                                <TextBlock Text="Descripcion" FontFamily="Tahoma" FontSize="9" 
                                           Foreground="#404040" Margin="0,0,0,3"/>
                                <TextBox Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource LegacyTextBoxStyle}"
                                         FontFamily="Tahoma" FontSize="9" Height="60"
                                         TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"/>
                            </StackPanel>
                            
                            <Grid Margin="0,12,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Fecha Inicio" FontFamily="Tahoma" FontSize="9" 
                                               Foreground="#404040" Margin="0,0,0,3"/>
                                    <DatePicker SelectedDate="{Binding FechaInicio}" 
                                                FontFamily="Tahoma" FontSize="9" Height="22"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Fecha Fin" FontFamily="Tahoma" FontSize="9" 
                                               Foreground="#404040" Margin="0,0,0,3"/>
                                    <DatePicker SelectedDate="{Binding FechaFin}" 
                                                FontFamily="Tahoma" FontSize="9" Height="22"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="4">
                                    <TextBlock Text="Estado" FontFamily="Tahoma" FontSize="9" 
                                               Foreground="#404040" Margin="0,0,0,3"/>
                                    <ComboBox ItemsSource="{Binding EstadosForm}"
                                              SelectedValuePath="Valor"
                                              DisplayMemberPath="Nombre"
                                              SelectedValue="{Binding Estado}"
                                              FontFamily="Tahoma" FontSize="9" Height="22"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                    
                    <!-- Botones -->
                    <Border Grid.Row="2" Background="#E0E0E0" BorderThickness="0,1,0,0" BorderBrush="#808080" Padding="10">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <controls:LegacyAsyncButton Content="Cancelar" 
                                    Command="{Binding CancelEditCommand}" Margin="0,0,8,0"/>
                            <controls:LegacyAsyncButton Content="Guardar" 
                                    Command="{Binding SaveProyectoCommand}"
                                    IsLoading="{Binding IsLoading}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
        
        <!-- ============================================ -->
        <!-- VISTA DE LISTA -->
        <!-- ============================================ -->
        <Grid Visibility="{Binding IsListViewVisible, Converter={StaticResource BoolToVisConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" Background="#000080" Padding="10,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Content="&lt;- Volver" 
                            Command="{Binding ShowHomeCommand}"
                            Style="{StaticResource LegacyButtonStyle}"
                            FontFamily="Tahoma" FontSize="9" Padding="10,3" Margin="0,0,15,0"/>
                    
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="[LISTA] PROYECTOS" FontFamily="Courier New" FontSize="12" 
                                   FontWeight="Bold" Foreground="White"/>
                        <TextBlock FontFamily="Tahoma" FontSize="9" Foreground="#C0C0FF" Margin="0,2,0,0">
                            <Run Text="Total: "/>
                            <Run Text="{Binding TotalProyectos, Mode=OneWay}"/>
                            <Run Text=" proyecto(s)"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Filtros -->
            <Border Grid.Row="1" Background="#E0E0E0" BorderThickness="0,0,0,1" BorderBrush="#808080" Padding="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Grid.Column="0" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource LegacyTextBoxStyle}"
                             FontFamily="Tahoma" FontSize="9" Height="22" Margin="0,0,8,0"/>
                    
                    <ComboBox Grid.Column="1" Width="120" Margin="0,0,8,0"
                              ItemsSource="{Binding Estados}"
                              SelectedValuePath="Valor"
                              DisplayMemberPath="Nombre"
                              SelectedValue="{Binding SelectedEstado}"
                              FontFamily="Tahoma" FontSize="9" Height="22"/>
                    
                    <Button Grid.Column="2" Content="Buscar" 
                            Command="{Binding SearchProyectosCommand}"
                            Style="{StaticResource LegacyButtonStyle}"
                            FontFamily="Tahoma" FontSize="9" Padding="10,3"/>
                    
                    <Button Grid.Column="3" Content="X" Width="22" Margin="5,0,0,0"
                            Command="{Binding ClearFiltersCommand}"
                            Style="{StaticResource LegacyButtonStyle}"
                            FontFamily="Tahoma" FontSize="9" FontWeight="Bold"
                            ToolTip="Limpiar filtros"/>
                </Grid>
            </Border>
            
            <!-- DataGrid de proyectos -->
            <Border Grid.Row="2" BorderThickness="1" BorderBrush="#808080" Margin="8" Background="White">
                <DataGrid ItemsSource="{Binding Proyectos}"
                          SelectedItem="{Binding SelectedProyecto}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="All"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          Background="White"
                          RowBackground="White"
                          AlternatingRowBackground="#E8E8E8"
                          FontFamily="Tahoma" FontSize="9"
                          ColumnHeaderHeight="22"
                          RowHeight="20">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#000080"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontFamily" Value="Tahoma"/>
                            <Setter Property="FontSize" Value="9"/>
                            <Setter Property="Padding" Value="5,3"/>
                            <Setter Property="BorderBrush" Value="#404040"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="4,2"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#000080"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Codigo" Binding="{Binding Codigo}" Width="80"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="120"/>
                        <DataGridTemplateColumn Header="Estado" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Center" FontFamily="Tahoma" FontSize="9">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Activo"/>
                                                <Setter Property="Foreground" Value="#008000"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Estado}" Value="1">
                                                        <Setter Property="Text" Value="Suspendido"/>
                                                        <Setter Property="Foreground" Value="#CC6600"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Estado}" Value="2">
                                                        <Setter Property="Text" Value="Finalizado"/>
                                                        <Setter Property="Foreground" Value="#000080"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Estado}" Value="3">
                                                        <Setter Property="Text" Value="Cancelado"/>
                                                        <Setter Property="Foreground" Value="#CC0000"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Acciones" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Ed" ToolTip="Editar"
                                                Command="{Binding DataContext.EditProyectoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                Style="{StaticResource LegacyButtonStyle}"
                                                FontFamily="Tahoma" FontSize="8" Width="30" Padding="0,2" Margin="0,0,3,0"/>
                                        <Button Content="X" ToolTip="Eliminar"
                                                Command="{Binding DataContext.DeleteProyectoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                Style="{StaticResource LegacyButtonStyle}"
                                                FontFamily="Tahoma" FontSize="8" Width="24" Padding="0,2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            
            <!-- Barra de estado -->
            <Border Grid.Row="3" Background="#E0E0E0" BorderThickness="0,1,0,0" BorderBrush="#808080" Padding="8,5">
                <TextBlock Text="{Binding TotalProyectos, StringFormat='{}{0} proyecto(s)'}" 
                           HorizontalAlignment="Right" FontFamily="Tahoma" FontSize="9" Foreground="#404040"/>
            </Border>
        </Grid>
        
        <!-- Loading overlay -->
        <Grid Background="#80000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">
            <Border Background="#C0C0C0" BorderThickness="2" BorderBrush="#808080" Padding="25" 
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="[...] Cargando" FontFamily="Courier New" FontSize="12" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <ProgressBar IsIndeterminate="True" Width="150" Height="18"/>
                    <TextBlock Text="Por favor espere..." FontFamily="Tahoma" FontSize="9"
                               HorizontalAlignment="Center" Foreground="#404040" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
