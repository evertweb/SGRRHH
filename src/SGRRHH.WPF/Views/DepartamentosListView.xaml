<UserControl x:Class="SGRRHH.WPF.Views.DepartamentosListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Gestión de Departamentos" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Administre los departamentos de la organización" FontSize="14" Foreground="#B3FFFFFF" Margin="0,5,0,0"/>
                </StackPanel>
                
                <Button Grid.Column="1" Command="{Binding NuevoCommand}" 
                        Background="#4CAF50" Foreground="White" 
                        Padding="20,10" FontSize="14" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontSize="18" FontWeight="Bold" Margin="0,0,8,0"/>
                        <TextBlock Text="Nuevo Departamento" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
        
        <!-- Formulario de edición -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>
            
            <StackPanel>
                <TextBlock Text="{Binding EditingId, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter='Editar Departamento|Nuevo Departamento'}" 
                           FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"
                           Foreground="#333"/>
                <TextBlock FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="#333">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="Nuevo Departamento"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EditingId, Converter={StaticResource NotNullToVisibilityConverter}}" Value="Visible">
                                    <Setter Property="Text" Value="Editar Departamento"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Nombre -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Nombre *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" 
                                 Padding="10" FontSize="14"/>
                    </StackPanel>
                    
                    <!-- Descripción -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Descripción" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}" 
                                 Padding="10" FontSize="14"/>
                    </StackPanel>
                    
                    <!-- Activo -->
                    <StackPanel Grid.Column="4" VerticalAlignment="Bottom">
                        <CheckBox Content="Activo" IsChecked="{Binding Activo}" 
                                  FontSize="14" Margin="0,0,0,10"/>
                    </StackPanel>
                </Grid>
                
                <!-- Botones -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button Content="Cancelar" Command="{Binding CancelarCommand}"
                            Padding="20,10" Margin="0,0,10,0" Background="#9E9E9E" 
                            Foreground="White" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="Guardar" Command="{Binding GuardarCommand}"
                            Padding="20,10" Background="#4CAF50" 
                            Foreground="White" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <!-- Lista de departamentos -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Encabezado de tabla -->
                <Border Grid.Row="0" Background="#F5F5F5" Padding="15">
                    <TextBlock Text="Lista de Departamentos" FontWeight="SemiBold" FontSize="14"/>
                </Border>
                
                <!-- DataGrid -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Departamentos}" 
                          SelectedItem="{Binding SelectedDepartamento}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          SelectionMode="Single" SelectionUnit="FullRow"
                          GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#E0E0E0"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          RowHeight="50"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          Background="White">
                    
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#37474F"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="#455A64"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="12,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="#212121"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="2*"/>
                        <DataGridTemplateColumn Header="Estado" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="8,4" CornerRadius="4" HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Activo}" Value="False">
                                                        <Setter Property="Background" Value="#FFEBEE"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock HorizontalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Activo"/>
                                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Activo}" Value="False">
                                                            <Setter Property="Text" Value="Inactivo"/>
                                                            <Setter Property="Foreground" Value="#F44336"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Acciones" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="✏️ Editar" 
                                                Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                Background="#2196F3" Foreground="White" 
                                                Padding="12,6" Margin="0,0,5,0" 
                                                BorderThickness="0" Cursor="Hand"
                                                FontSize="12" FontWeight="SemiBold"/>
                                        <Button Content="🗑️ Eliminar" 
                                                Command="{Binding DataContext.EliminarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                Background="#F44336" Foreground="White" 
                                                Padding="12,6"
                                                BorderThickness="0" Cursor="Hand"
                                                FontSize="12" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                
                <!-- Loading overlay -->
                <Border Grid.Row="1" Background="#80FFFFFF" 
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Cargando..." FontSize="16" Foreground="#666"/>
                    </StackPanel>
                </Border>
                
                <!-- Empty state -->
                <Border Grid.Row="1" Background="White"
                        Visibility="{Binding Departamentos.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="📁" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="No hay departamentos registrados" FontSize="16" Foreground="#999"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        
        <!-- Error message -->
        <Border Grid.Row="2" VerticalAlignment="Top" Margin="0,60,0,0" Background="#FFEBEE" 
                CornerRadius="4" Padding="15"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NotNullToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#D32F2F"/>
        </Border>
    </Grid>
</UserControl>
