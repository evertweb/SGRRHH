<Window x:Class="SGRRHH.WPF.Views.CambiarPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cambiar Contraseña" 
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="🔑 Cambiar Contraseña" FontSize="18" FontWeight="Bold" 
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Contraseña Actual *" Margin="0,0,0,5" Foreground="{StaticResource TextSecondaryBrush}"/>
            <PasswordBox x:Name="PasswordActual" Padding="8" FontSize="14"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Nueva Contraseña *" Margin="0,0,0,5" Foreground="{StaticResource TextSecondaryBrush}"/>
            <PasswordBox x:Name="PasswordNueva" Padding="8" FontSize="14"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Confirmar Nueva Contraseña *" Margin="0,0,0,5" Foreground="{StaticResource TextSecondaryBrush}"/>
            <PasswordBox x:Name="PasswordConfirmar" Padding="8" FontSize="14"/>
        </StackPanel>
        
        <!-- Mensaje de error -->
        <TextBlock Grid.Row="4" 
                   Text="{Binding Mensaje}" 
                   Foreground="{StaticResource DangerBrush}" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"
                   Visibility="{Binding Mensaje, Converter={StaticResource NullToVisibilityConverter}}"/>
        
        <!-- Botones -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" 
                    Style="{StaticResource SecondaryButton}"
                    Padding="20,8" 
                    Margin="0,0,10,0"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
            <Button Content="💾 Cambiar Contraseña" 
                    Style="{StaticResource SuccessButton}"
                    Padding="20,8"
                    Click="ChangePasswordButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
