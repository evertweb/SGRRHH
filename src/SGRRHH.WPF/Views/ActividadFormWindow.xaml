<Window x:Class="SGRRHH.WPF.Views.ActividadFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="420" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#C0C0C0">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles/LegacyTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Título -->
        <Border Grid.Row="0" Background="#000080" Padding="10,6">
            <TextBlock Text="{Binding WindowTitle}" FontFamily="Tahoma" FontSize="11" 
                       FontWeight="Bold" Foreground="White"/>
        </Border>
        
        <!-- Formulario -->
        <Border Grid.Row="1" Background="White" BorderThickness="1" BorderBrush="#808080" Margin="10,10,10,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="15">
                <StackPanel>
                    <!-- Código -->
                    <TextBlock Text="Codigo *" FontFamily="Tahoma" FontSize="9" 
                               Foreground="#404040" Margin="0,0,0,3"/>
                    <TextBox Text="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource LegacyTextBoxStyle}"
                             FontFamily="Tahoma" FontSize="9" Height="22"
                             IsReadOnly="True" Background="#E0E0E0" Margin="0,0,0,10"/>
                    
                    <!-- Nombre -->
                    <TextBlock Text="Nombre *" FontFamily="Tahoma" FontSize="9" 
                               Foreground="#404040" Margin="0,0,0,3"/>
                    <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource LegacyTextBoxStyle}"
                             FontFamily="Tahoma" FontSize="9" Height="22"
                             MaxLength="100" Margin="0,0,0,10"/>
                    
                    <!-- Categoría -->
                    <TextBlock Text="Categoria *" FontFamily="Tahoma" FontSize="9" 
                               Foreground="#404040" Margin="0,0,0,3"/>
                    <ComboBox ItemsSource="{Binding CategoriasDisponibles}"
                              SelectedItem="{Binding CategoriaSeleccionada}"
                              FontFamily="Tahoma" FontSize="9" Height="22" Margin="0,0,0,10"/>
                    
                    <!-- Descripción -->
                    <TextBlock Text="Descripcion" FontFamily="Tahoma" FontSize="9" 
                               Foreground="#404040" Margin="0,0,0,3"/>
                    <TextBox Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource LegacyTextBoxStyle}"
                             FontFamily="Tahoma" FontSize="9" Height="60"
                             TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalContentAlignment="Top" MaxLength="500" Margin="0,0,0,10"/>
                    
                    <!-- Orden y Requiere Proyecto -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Orden de visualizacion" FontFamily="Tahoma" FontSize="9" 
                                       Foreground="#404040" Margin="0,0,0,3"/>
                            <TextBox Text="{Binding Orden, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource LegacyTextBoxStyle}"
                                     FontFamily="Tahoma" FontSize="9" Height="22"
                                     HorizontalContentAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Requiere proyecto?" FontFamily="Tahoma" FontSize="9" 
                                       Foreground="#404040" Margin="0,0,0,3"/>
                            <CheckBox IsChecked="{Binding RequiereProyecto}" 
                                      Content="Si, debe seleccionar proyecto" 
                                      FontFamily="Tahoma" FontSize="9" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Info -->
                    <Border Background="#FFFFC0" BorderThickness="1" BorderBrush="#808080" Padding="8" Margin="0,5,0,0">
                        <TextBlock TextWrapping="Wrap" FontFamily="Tahoma" FontSize="8" Foreground="#404040">
                            <Run FontWeight="Bold">[!] Tip:</Run>
                            <Run Text=" Si la actividad requiere proyecto, el usuario debera seleccionar un proyecto al registrarla en el control diario."/>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
        
        <!-- Botones -->
        <Border Grid.Row="2" Background="#E0E0E0" BorderThickness="0,1,0,0" BorderBrush="#808080" Padding="10,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar" Width="80"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource LegacyButtonStyle}"
                        FontFamily="Tahoma" FontSize="9" Padding="0,4" Margin="0,0,8,0"/>
                <Button Content="Guardar" Width="80"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource LegacyButtonStyle}"
                        FontFamily="Tahoma" FontSize="9" Padding="0,4"/>
            </StackPanel>
        </Border>
        
        <!-- Loading overlay -->
        <Border Grid.RowSpan="3" Background="#80FFFFFF" 
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">
            <Border Background="White" BorderThickness="2" BorderBrush="#808080" 
                    Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="[...]" FontFamily="Courier New" FontSize="20" FontWeight="Bold"
                               Foreground="#000080" HorizontalAlignment="Center"/>
                    <TextBlock Text="Guardando..." FontFamily="Tahoma" FontSize="10" 
                               Foreground="#808080" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
