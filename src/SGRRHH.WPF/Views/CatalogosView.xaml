<UserControl x:Class="SGRRHH.WPF.Views.CatalogosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#FAFAFA">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Encabezado -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="📁 Catálogos del Sistema" FontSize="24" FontWeight="Bold" Foreground="#37474F"/>
            <TextBlock Text="Seleccione una categoría para administrar los datos maestros" 
                       FontSize="14" Foreground="#757575" Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- Cards de catálogos -->
        <WrapPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top">
            
            <!-- Departamentos -->
            <Border Background="White" CornerRadius="12" Padding="25" Margin="10" Width="220" Height="180"
                    BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" BlurRadius="10" ShadowDepth="0" Opacity="0.1"/>
                </Border.Effect>
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToDepartamentosCommand}"/>
                </Border.InputBindings>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="🏢" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Departamentos" FontSize="16" FontWeight="SemiBold" 
                               Foreground="#1565C0" HorizontalAlignment="Center"/>
                    <TextBlock Text="Gestión de áreas organizacionales" FontSize="11" 
                               Foreground="#757575" HorizontalAlignment="Center" TextWrapping="Wrap"
                               TextAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Cargos -->
            <Border Background="White" CornerRadius="12" Padding="25" Margin="10" Width="220" Height="180"
                    BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" BlurRadius="10" ShadowDepth="0" Opacity="0.1"/>
                </Border.Effect>
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToCargosCommand}"/>
                </Border.InputBindings>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="💼" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Cargos" FontSize="16" FontWeight="SemiBold" 
                               Foreground="#2E7D32" HorizontalAlignment="Center"/>
                    <TextBlock Text="Puestos y funciones laborales" FontSize="11" 
                               Foreground="#757575" HorizontalAlignment="Center" TextWrapping="Wrap"
                               TextAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Proyectos -->
            <Border Background="White" CornerRadius="12" Padding="25" Margin="10" Width="220" Height="180"
                    BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" BlurRadius="10" ShadowDepth="0" Opacity="0.1"/>
                </Border.Effect>
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToProyectosCommand}"/>
                </Border.InputBindings>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="🚀" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Proyectos" FontSize="16" FontWeight="SemiBold" 
                               Foreground="#E65100" HorizontalAlignment="Center"/>
                    <TextBlock Text="Gestión de proyectos activos" FontSize="11" 
                               Foreground="#757575" HorizontalAlignment="Center" TextWrapping="Wrap"
                               TextAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Actividades -->
            <Border Background="White" CornerRadius="12" Padding="25" Margin="10" Width="220" Height="180"
                    BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" BlurRadius="10" ShadowDepth="0" Opacity="0.1"/>
                </Border.Effect>
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToActividadesCommand}"/>
                </Border.InputBindings>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="📝" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Actividades" FontSize="16" FontWeight="SemiBold" 
                               Foreground="#7B1FA2" HorizontalAlignment="Center"/>
                    <TextBlock Text="Tipos de actividades laborales" FontSize="11" 
                               Foreground="#757575" HorizontalAlignment="Center" TextWrapping="Wrap"
                               TextAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Tipos de Permiso -->
            <Border Background="White" CornerRadius="12" Padding="25" Margin="10" Width="220" Height="180"
                    BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" BlurRadius="10" ShadowDepth="0" Opacity="0.1"/>
                </Border.Effect>
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToTiposPermisoCommand}"/>
                </Border.InputBindings>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="📋" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Tipos de Permiso" FontSize="16" FontWeight="SemiBold" 
                               Foreground="#C62828" HorizontalAlignment="Center"/>
                    <TextBlock Text="Categorías de permisos laborales" FontSize="11" 
                               Foreground="#757575" HorizontalAlignment="Center" TextWrapping="Wrap"
                               TextAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            
        </WrapPanel>
    </Grid>
</UserControl>
