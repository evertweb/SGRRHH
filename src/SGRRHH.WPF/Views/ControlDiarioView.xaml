<UserControl x:Class="SGRRHH.WPF.Views.ControlDiarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="White">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Encabezado con selección de empleado y fecha -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="15" CornerRadius="8" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Selector de empleado -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Empleado:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                    <ComboBox Width="300" Height="35" 
                              ItemsSource="{Binding Empleados}" 
                              SelectedItem="{Binding SelectedEmpleado}"
                              DisplayMemberPath="NombreCompleto"
                              VerticalContentAlignment="Center"/>
                </StackPanel>
                
                <!-- Navegación de fecha -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0">
                    <Button Content="◀" Width="35" Height="35" Command="{Binding DiaAnteriorCommand}"
                            Background="#1E88E5" Foreground="White" BorderThickness="0"/>
                    <DatePicker SelectedDate="{Binding SelectedFecha}" Width="130" Height="35" 
                                Margin="5,0" VerticalContentAlignment="Center"/>
                    <Button Content="▶" Width="35" Height="35" Command="{Binding DiaSiguienteCommand}"
                            Background="#1E88E5" Foreground="White" BorderThickness="0"/>
                    <Button Content="Hoy" Width="60" Height="35" Margin="10,0,0,0"
                            Command="{Binding IrAHoyCommand}"
                            Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                </StackPanel>
                
                <!-- Estadísticas rápidas -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Border Background="#E3F2FD" CornerRadius="5" Padding="10,5" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Horas hoy: " FontSize="12"/>
                            <TextBlock Text="{Binding TotalHoras, StringFormat=N1}" FontWeight="Bold" FontSize="12"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FFF3E0" CornerRadius="5" Padding="10,5" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Mes: " FontSize="12"/>
                            <TextBlock Text="{Binding HorasMesActual, StringFormat=N1}" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Text=" hrs" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Contenido principal -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>
            
            <!-- Panel izquierdo: Registro y actividades -->
            <Grid Grid.Column="0" Margin="0,0,15,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Horas de entrada/salida -->
                <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                        CornerRadius="8" Padding="15" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Hora de entrada" FontSize="12" Foreground="#757575"/>
                            <TextBox Text="{Binding HoraEntrada, StringFormat=hh\\:mm}" 
                                     Height="35" Margin="0,5,0,0" VerticalContentAlignment="Center"
                                     IsEnabled="{Binding CanEdit}"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Margin="10,0">
                            <TextBlock Text="Hora de salida" FontSize="12" Foreground="#757575"/>
                            <TextBox Text="{Binding HoraSalida, StringFormat=hh\\:mm}" 
                                     Height="35" Margin="0,5,0,0" VerticalContentAlignment="Center"
                                     IsEnabled="{Binding CanEdit}"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2" Margin="10,0">
                            <TextBlock Text="Observaciones" FontSize="12" Foreground="#757575"/>
                            <TextBox Text="{Binding Observaciones, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="35" Margin="0,5,0,0" VerticalContentAlignment="Center"
                                     IsEnabled="{Binding CanEdit}"/>
                        </StackPanel>
                        
                        <Button Grid.Column="3" Content="💾 Guardar" Width="100" Height="35"
                                Command="{Binding SaveRegistroCommand}"
                                Background="#1E88E5" Foreground="White" BorderThickness="0"
                                VerticalAlignment="Bottom" Margin="10,0,0,0"
                                IsEnabled="{Binding CanEdit}"/>
                    </Grid>
                </Border>
                
                <!-- Agregar actividad -->
                <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                        CornerRadius="8" Padding="15" Margin="0,0,0,15"
                        IsEnabled="{Binding CanEdit}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,10,0">
                            <TextBlock Text="Actividad *" FontSize="12" Foreground="#757575"/>
                            <ComboBox ItemsSource="{Binding Actividades}" 
                                      SelectedItem="{Binding SelectedActividad}"
                                      Height="35" Margin="0,5,0,0">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Nombre}"/>
                                            <TextBlock Text=" - " Foreground="#9E9E9E"/>
                                            <TextBlock Text="{Binding Categoria}" Foreground="#9E9E9E" FontSize="11"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Grid.Row="0" Margin="10,0">
                            <TextBlock FontSize="12" Foreground="#757575">
                                <Run Text="Proyecto"/>
                                <Run x:Name="RequiredAsterisk" Text=" *" Foreground="#E53935"/>
                            </TextBlock>
                            <ComboBox ItemsSource="{Binding Proyectos}" 
                                      SelectedItem="{Binding SelectedProyecto}"
                                      DisplayMemberPath="Nombre"
                                      Height="35" Margin="0,5,0,0"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2" Grid.Row="0" Margin="10,0">
                            <TextBlock Text="Horas *" FontSize="12" Foreground="#757575"/>
                            <TextBox Text="{Binding Horas, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="35" Margin="0,5,0,0" VerticalContentAlignment="Center"
                                     HorizontalContentAlignment="Center"/>
                        </StackPanel>
                        
                        <Button Grid.Column="3" Grid.Row="0" Content="➕" Width="40" Height="35"
                                Command="{Binding AddActividadCommand}"
                                Background="#4CAF50" Foreground="White" BorderThickness="0"
                                FontSize="16" VerticalAlignment="Bottom" Margin="10,0,0,0"
                                ToolTip="Agregar actividad"/>
                        
                        <StackPanel Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="1" Margin="0,10,0,0">
                            <TextBlock Text="Descripción (opcional)" FontSize="12" Foreground="#757575"/>
                            <TextBox Text="{Binding DescripcionActividad, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="35" Margin="0,5,0,0" VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Lista de actividades del día -->
                <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                        CornerRadius="8" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="📋 Actividades del día" FontWeight="SemiBold" FontSize="14" 
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding TotalHoras, StringFormat=' ({0:N1} horas)'}" 
                                       Foreground="#757575" VerticalAlignment="Center"/>
                            <Button Content="🗑️" Width="35" Height="30" Margin="15,0,0,0"
                                    Command="{Binding DeleteActividadCommand}"
                                    Background="#FFEBEE" Foreground="#E53935" BorderThickness="0"
                                    ToolTip="Eliminar actividad seleccionada"
                                    IsEnabled="{Binding CanEdit}"/>
                        </StackPanel>
                        
                        <DataGrid Grid.Row="1" 
                                  ItemsSource="{Binding DetallesActividades}"
                                  SelectedItem="{Binding SelectedDetalle}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  BorderThickness="0"
                                  RowHeight="40">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#" Binding="{Binding Orden}" Width="40"/>
                                <DataGridTextColumn Header="Actividad" Binding="{Binding Actividad.Nombre}" Width="*"/>
                                <DataGridTextColumn Header="Proyecto" Binding="{Binding Proyecto.Nombre}" Width="150"/>
                                <DataGridTextColumn Header="Horas" Binding="{Binding Horas, StringFormat=N1}" Width="70"/>
                                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="200"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- Panel derecho: Historial y acciones -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Acciones -->
                <Border Grid.Row="0" Background="#E8F5E9" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="⚡ Acciones" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>
                        
                        <Button Content="✅ Completar Registro" Height="40"
                                Command="{Binding CompletarRegistroCommand}"
                                Background="#4CAF50" Foreground="White" BorderThickness="0"
                                IsEnabled="{Binding CanEdit}"/>
                        
                        <TextBlock Text="Una vez completado, el registro no podrá modificarse."
                                   FontSize="11" Foreground="#757575" TextWrapping="Wrap" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Historial reciente -->
                <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                        CornerRadius="8" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="📅 Registros del mes" FontWeight="SemiBold" FontSize="14" 
                                   Margin="0,0,0,10"/>
                        
                        <ListBox Grid.Row="1" ItemsSource="{Binding RegistrosRecientes}"
                                 BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="5" Margin="0,0,0,5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" 
                                                       Text="{Binding Fecha, StringFormat=dd/MM/yyyy ddd}"
                                                       FontSize="12"/>
                                            
                                            <TextBlock Grid.Column="1" 
                                                       Text="{Binding TotalHoras, StringFormat={}{0:N1}h}"
                                                       FontWeight="SemiBold" Margin="10,0" FontSize="12"/>
                                            
                                            <Border Grid.Column="2" CornerRadius="3" Padding="5,2">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#FFF3E0"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Estado}" Value="1">
                                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Estado}" Value="2">
                                                                <Setter Property="Background" Value="#E3F2FD"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock FontSize="10">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="Borrador"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Estado}" Value="1">
                                                                    <Setter Property="Text" Value="Completado"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Estado}" Value="2">
                                                                    <Setter Property="Text" Value="Aprobado"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
        
        <!-- Barra de estado -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10" Margin="0,15,0,0" CornerRadius="5">
            <Grid>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#757575" VerticalAlignment="Center"/>
                <ProgressBar IsIndeterminate="True" Width="100" Height="3" HorizontalAlignment="Right"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
