<Window x:Class="SGRRHH.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SGRRHH - Iniciar Sesión"
        Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Border Background="#FAFAFA" CornerRadius="10" 
            BorderBrush="#1E88E5" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Barra de título -->
            <Border Grid.Row="0" Background="#1E88E5" CornerRadius="8,8,0,0" 
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="15,10">
                    <TextBlock Text="SGRRHH" FontSize="18" FontWeight="Bold" 
                               Foreground="White" HorizontalAlignment="Left"/>
                    <Button Content="✕" HorizontalAlignment="Right" 
                            Background="Transparent" BorderThickness="0"
                            Foreground="White" FontSize="16" Cursor="Hand"
                            Command="{Binding ExitCommand}"
                            Padding="5,0"/>
                </Grid>
            </Border>
            
            <!-- Contenido -->
            <StackPanel Grid.Row="1" Margin="40,30" VerticalAlignment="Center">
                
                <!-- Logo/Título -->
                <TextBlock Text="👤" FontSize="60" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Iniciar Sesión" FontSize="24" FontWeight="SemiBold" 
                           Foreground="#37474F" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <!-- Usuario -->
                <TextBlock Text="Usuario" Foreground="#37474F" FontSize="13" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Padding="10" FontSize="14" Margin="0,0,0,15"
                         BorderBrush="#BDBDBD" BorderThickness="1">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#1E88E5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                
                <!-- Contraseña -->
                <TextBlock Text="Contraseña" Foreground="#37474F" FontSize="13" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" 
                             Padding="10" FontSize="14" Margin="0,0,0,15"
                             BorderBrush="#BDBDBD" BorderThickness="1"
                             PasswordChanged="PasswordBox_PasswordChanged">
                    <PasswordBox.Style>
                        <Style TargetType="PasswordBox">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#1E88E5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </PasswordBox.Style>
                </PasswordBox>
                
                <!-- Mensaje de error -->
                <Border Background="#FFEBEE" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                        Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#E53935" 
                               TextWrapping="Wrap" FontSize="12"/>
                </Border>
                
                <!-- Botón de Login -->
                <Button Content="Iniciar Sesión" 
                        Command="{Binding LoginCommand}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                        Padding="15,12" FontSize="14" FontWeight="SemiBold"
                        Cursor="Hand" Margin="0,10,0,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#1E88E5"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1976D2"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#BDBDBD"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- Indicador de carga -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" 
                            Margin="0,15,0,0"
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="⏳ Verificando credenciales..." Foreground="#757575" FontSize="12"/>
                </StackPanel>
                
            </StackPanel>
        </Grid>
    </Border>
</Window>
